import OpsDemoSlip from "@/components/inspired-closets/OpsDemoSlip";

export const metadata = {
  title: "Inspired Closets OS · Demo packing slip",
};

export default function InspiredClosetsDemoSlipPage() {
  return <OpsDemoSlip />;
}
