import OpsInventoryWorkspace from "@/components/inspired-closets/OpsInventoryWorkspace";

export const metadata = {
  title: "Inspired Closets OS · Inventory",
};

export default function InspiredClosetsOpsInventoryPage() {
  return <OpsInventoryWorkspace />;
}
